{
  "name": "hbuhb",
  "version": "0.1.0",
  "scripts": {
    "back": "cd back && npm start",
    "front": "cd front && npm start",
    "start": "concurrently --kill-others-on-fail --names 'back,front' 'npm run back' 'PORT=3001 npm run front'"
  },
  "dependencies": {
    "concurrently": "^6.2.1",
    "reactjs-popup": "^2.0.5"
  }
}
